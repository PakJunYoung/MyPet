<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inhatc.mypet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pet_group_list"></activity>
        <activity android:name=".pet_group" />
        <activity android:name=".pet_info_list" />
        <activity android:name=".pet_info" />
        <activity android:name=".pet_upd" />
        <activity android:name=".pet_upd_select" />
        <activity
            android:name=".pet_del"
            android:label="펫 삭제" />
        <activity
            android:name=".pet_add"
            android:label="펫 추가" />
        <activity android:name=".listview_item" />
        <activity
            android:name=".main"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".join" />
    </application>

</manifest>