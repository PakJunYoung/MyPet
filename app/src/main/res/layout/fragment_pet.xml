<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".pet">

    <android.support.v4.widget.SwipeRefreshLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/swipe_layout">
    <ListView
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
    </android.support.v4.widget.SwipeRefreshLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:backgroundTint="#FF6666"
        app:srcCompat="@drawable/ic_pet_fab_main"
        app:maxImageSize="@dimen/fab_margin"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginRight="16dp"
        android:layout_marginBottom="80dp"
        android:backgroundTint="#FF6666"
        android:visibility="invisible"
        app:srcCompat="@drawable/ic_pet_fab_add"
        app:maxImageSize="@dimen/fab_image"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_del"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="134dp"
        android:layout_marginRight="16dp"
        android:visibility="invisible"
        android:backgroundTint="#FF6666"
        app:srcCompat="@drawable/ic_pet_fab_del"
        app:maxImageSize="@dimen/fab_image"
        />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_update"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="188dp"
        android:layout_marginRight="16dp"
        android:visibility="invisible"
        android:backgroundTint="#FF6666"
        app:srcCompat="@drawable/ic_pet_fab_upd"
        app:maxImageSize="@dimen/fab_image"
        />

</FrameLayout>